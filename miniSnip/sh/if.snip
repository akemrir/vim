? if
# FILE=/etc/docker
# if [ -d "$FILE" ]; then
#     echo "$FILE is a directory."
# if [ -f "$FILE" ]; then
#     echo "$FILE exists."
if <{[ "hello" == "hi" ] || [ 1 -gt 2 ]}>;
then
  <{echo "if 2"}>
else
  echo "if not 2"
fi
