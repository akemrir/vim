? Create type if not exist
-- +goose StatementBegin
do $$
begin
  if not exists (select 1 from pg_type where typname = '<{name}>') then
    create type <{~1}> as (<{token text}>);
  end if;
end
$$;
-- +goose StatementEnd
